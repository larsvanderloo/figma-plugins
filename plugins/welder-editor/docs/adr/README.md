# Plugin-specific ADRs

Architecture Decision Records scoped to this plugin. Cross-cutting decisions go in the repo root `docs/adr/` instead.

Format: `NNNN-<title>.md` (4-digit zero-padded sequence).

Required sections per ADR: Context · Decision · Alternatives considered · Consequences · References.
